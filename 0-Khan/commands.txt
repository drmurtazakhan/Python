Python and Anaconda are two different distribution os pythons
The of package manager of Anacondais known as conda.
----------------------------------------
On Windows command prompt writting python will start the Python interpreter
Python interpreter prompt is >>>
one can directly write python commmands on this prompt, e.g.,
print("Hello world!")

to exit from the python
>>>exit()
----------------------------------------

check python version

>python --version

upgrade pip
>py -m pip install --upgrade pip

install  packages
>py -m pip install numpy
>py -m pip install pandas
>py -m pip install matplotlib
>pip install csv

----------------------------------------
upgrade numpy
>pip install --upgrade numpy

>pip install jedi

>conda install spyder-kernels=2.5

> !pip install xgboost

>conda install xgboost

!conda install xgboost